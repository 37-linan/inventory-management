# 2026-08-17 工作日志

## 8-19/8-20 补充
- **价格历史 bug 修复**：PostgreSQL DATE 字段 pg 驱动返回 Date 对象，前端字符串匹配失败 → price-history 接口用 `to_char(date,'YYYY-MM-DD') as date` 转字符串
- **价格字符串 toFixed bug**：后端 price 返回字符串 "29.00"，chart.js 的 `p.val.toFixed(2)` 报错 → 统一 `parseFloat(p.val).toFixed(2)`
- **价格自动延续**：chart.js 新增 `_fillMissingPrices()`，缺失日期自动沿用最近一次价格（用户规则：当天23点前没录价就沿用前一天），图表始终连续，不写数据库
- **入库单按订单号分组**（transactions.js _refreshInboundTable）：组头显示单号/时间/商品数/总数量/整单金额，可折叠展开、📊统计弹窗、删除整单；数量显示整数（_fmtQty，1.00→1）
- **利润规则**（routes/main-cloud.js /ledger）：成本 = 入库单整单金额（首个商品 purchase_price）÷ 该单总数量 × 出库数量（FIFO 分摊）；收入 = 信息表单价（price_history 最新价）× 出库数量

## 8-18 补充（登录维护）
- 渠道配置：主系统 channel_options 改为 [淘宝,京东,拼多多,抖音,美团,唯品会,微信小程序,淘宝闪购,百度直播,支付宝惊喜市集]（去掉"线下采购"，新增6个电商渠道）
- **抖音刷券系统彻底移除**（用户说不用管了）：
  - index.html 删除 douyin-nav-group + douyin 页面容器
  - app.js 删除 douyin 路由 case
  - 注意：主系统产品类型里可能还有历史"抖音刷券"类型产品，未处理（用户未要求）

## HTTPS 部署（扫码质量根本提升）
- 背景：HTTP 环境手机无法实时扫码（浏览器安全限制），拍照扫码质量差
- 用户注册域名：**nanyishangmao.cn**（腾讯云，.cn 续费 38 元/年）
- 服务器：腾讯云轻量应用服务器 **Ubuntu-c2EV**（广州），实例 ins-59s0u7vq，IP 211.159.186.87
  - 注意：轻量应用服务器端口放行用"**防火墙**"标签（不是云服务器的"安全组"）
- 已完成：
  - ✅ 域名实名认证通过（所有者李楠）
  - ✅ DNS 解析 @ → 211.159.186.87（已生效）
  - ⏳ www 解析（用户要加）
  - ✅ 轻量服务器防火墙放行 80/443/22/3000
  - ✅ 安装 Caddy v2.11.4，Caddyfile 反代 127.0.0.1:3000，已启用运行
  - ⏳ ICP 备案：用户已提交（审核 10-20 工作日）
- 已知问题：未备案时腾讯云/运营商拦截 80 端口（返回 dnspod webblock 页），Let's Encrypt 证书签不下来
  - **备案通过后 Caddy 自动重试签证书，HTTPS 自动生效**
- 下一步：备案通过后 → https 生效 → 前端升级 ZXing 实时视频扫码（barcode.js 已支持 getUserMedia 实时检测，HTTPS 下自动启用）

## 扫码质量增强（8-16 晚）
- 用户条码为美团运单条码（Code128，19位数字，反光+密集），ZXing/Quagga 全部识别失败
- 已装服务端 **@zxing/library + jimp**（服务端 ZXing 解码，多尺度+TRY_HARDER+反色+灰度增强）
- 已装 **tesseract-ocr**（服务端 OCR 兜底 /api/ocr-text，多变体+投票+长度优先）
- 前端 barcode.js：ZXing→OCR 流程 + 识别结果**确认弹窗**（可修改再填入）+ 框选条码/数字区域功能
- 结论：HTTP 下拍照扫码只能优化不能根治，根治靠 HTTPS 实时扫码
