# 2026-07-29 工作日志

## 云端部署准备完成

### 已完成
1. 代码已上传 GitHub: https://github.com/37-linan/inventory-management
2. server.js 支持双模式：DATABASE_URL 环境变量切换云端/本地
3. 已部署到 Railway，网址: https://inventory-management-production-6c45.up.railway.app
4. PostgreSQL 数据库已连接

## 下午 - 套餐管理功能（主品+赠品）

### 改动内容
1. **数据库**: main_products 表增加 `gift_of` 字段
2. **后端**: main-cloud.js 产品 API 支持 gift_of 参数
3. **前端 products.js**:
   - 选择"抖音刷券"类型，保存主品后自动弹出赠品添加窗（选填）
   - 产品列表：赠品以子链接缩进显示在主品下方，带"赠品"标识
4. **pg-init.js**: 新部署/重启自动加 gift_of 字段 + 迁移脚本

### 说明
- 只改了云版本（main-cloud.js + pg-init.js + 前端），本地版本未动
- 数据都在云上，http://localhost:3000 的本地版本已废弃

## 深夜 - 腾讯云服务器部署（最终方案）

### 服务器信息
- 腾讯云轻量应用服务器：2核2G，68元/年
- IP：211.159.186.87
- SSH 用户：ubuntu（不是 root）
- SSH 密钥：/tmp/tencent_rsa

### 部署状态
- ✅ Node.js 22.23.2 已安装
- ✅ PostgreSQL 14 已安装
- ✅ PM2 进程管理 + 开机自启
- ✅ server.js 增加 .env 加载逻辑
- ✅ 数据库切换为 PostgreSQL（之前误用了 SQLite）

### ⚠️ 重要：部署注意事项
**绝对禁止：**
- ❌ `rm -f ~/inventory-app/db/*.db`（会删除所有数据）
- ❌ DROP TABLE 或 TRUNCATE
- ❌ 删除 PostgreSQL 数据库

**正确流程：**
1. 改代码 → 推 GitHub
2. SSH 下载新文件（curl 覆盖 .js）
3. `pm2 restart inventory-app`
4. 数据完整保留

### 访问地址
- http://211.159.186.87:3000（已在腾讯云防火墙放行 3000 端口）

## 凌晨 - 赠品样式大改 + 套组功能 + 多项修复

### 前端样式
- 赠品从满格表格改为 **嵌套 inline-flex div**，宽度自动收缩
- 赠品行左边加黄色竖线 + 浅色背景
- 赠品右侧加橙色"删除"按钮
- 屈臣氏类型下隐藏赠品（仅抖音刷券显示）
- 产品列表增加"套组"列

### 数据库
- 添加 `bundle_qty` 字段（默认1，抖音刷券倍数库存）
- 移除 code 唯一约束（允许相同编码多规格重复）
- 修复删除：改为 code+type 双重匹配（不同类型互不影响）
- 添加 gift_of 字段（赠品关联）

### 后端
- 库存统计：抖音刷券 = 入库数×套组数量；屈臣氏 = 入库数
- 添加产品：不检查重复，支持同编码多次添加
- INSERT 支持 bundle_qty 字段

### 用户偏好
- 用户名为 ubuntu，非 root
- 以后改代码只影响云版（腾讯云），不动本地 SQLite 版
- 部署用: ssh + curl下载文件 + pm2 restart
