# 2026-09-12

## 标注颜色按商品独立（修复）
- 问题: 入库表格按单号分组渲染时，整组所有行用 `items[0].row_color` 上色 → 标记任一商品整单变色
- 修复 (transactions.js `_refreshInboundTable`):
  - 明细行每行用自己的 `r.row_color`（标哪个商品只有那行变色）
  - 组头行仅当本单所有商品标同一种颜色才整单着色，否则默认
- sw.js 缓存版本 v3 → v4 强制刷新
- 教训: 网页 vs 原生App扫码是能力鸿沟，勿再往实时扫码堆OCR补丁（9/3 踩过，用户明确要求移除OCR兜底，扫码纯ZXing）
- 运维: SSH 22 常被网络路径拦截，换手机热点即恢复（本次同样）
